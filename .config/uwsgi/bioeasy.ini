[uwsgi]
chdir = /srv/bioeasy/
module = bioeasy.wsgi:application
home = /home/ubuntu/myvenv/

uid = deploy
gid = deploy

http = :8080

enable-threads = true
master = true
vacuum = true
pidfile = /tmp/bioeasy.pid
logto = /var/log/uwsgi/bioeasy/@(exec://date +%%Y-%%m-%%d).log
log-reopen = true